import { DynamicHeadersDirective } from './dynamic-headers.directive';

describe('DynamicHeadersDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHeadersDirective();
    expect(directive).toBeTruthy();
  });
});
